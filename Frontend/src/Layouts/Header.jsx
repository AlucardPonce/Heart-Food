import React from 'react';

const Header = () => {
    return (
        <header>
            {/* Aquí iría el contenido del header si es necesario */}
        </header>
    );
};

export default Header;